# ═══════════════════════════════════════════════
# Saathi Cloud Hub — Python Dependencies
# ═══════════════════════════════════════════════

# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0

# WebSocket
websockets>=13.0

# Authentication
PyJWT>=2.9.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.2.0

# Redis (message broker)
redis[hiredis]>=5.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Optional: Docker SDK (for cloud workspace)
# docker>=7.0.0

# Optional: System monitoring (for daemon)
# psutil>=6.0.0
